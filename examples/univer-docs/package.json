{
  "name": "univer-docs-example",
  "version": "0.0.1",
  "description": "Univer Docs example that builds to a self-contained bundle with OPFS persistence",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "cd ../.. && pnpm -r run build",
    "dev": "nuxt dev",
    "build": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "vue": "^3.5.32",
    "@univerjs/presets": "^0.18.0",
    "@univerjs/ui": "^0.18.0",
    "@stark-o/browser-runtime": "link:../../packages/browser-runtime",
    "@stark-o/shared": "link:../../packages/shared"
  },
  "devDependencies": {
    "nuxt": "^3.21.2",
    "vite": "^6.4.2",
    "vue-router": "^4.6.4"
  }
}
